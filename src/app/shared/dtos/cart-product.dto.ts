export interface CartProductDto {
  productId: string;
  productImageURL: string;
  productName: string;
  description: string;
  discountId: string;
  productPrice: number;
  productDiscountPrice: number;
  quantity: number;
}
